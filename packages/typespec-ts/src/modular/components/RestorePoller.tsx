/**
 * RestorePoller component.
 *
 * Note: The full RestorePoller JSX component is deferred.
 * The LRO restore poller generation involves deeply imperative ts-morph code.
 *
 * For now, the existing buildRestorePoller.ts continues to be used.
 */

export { buildRestorePoller } from "../buildRestorePoller.js";
